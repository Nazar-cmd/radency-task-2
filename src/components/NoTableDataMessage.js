import TableCell from "./TableCell";

const NoTableDataMessage = () => (
	<div className="table__row table__no_items">
		<TableCell>There are no notes!</TableCell>
	</div>
);

export default NoTableDataMessage;
